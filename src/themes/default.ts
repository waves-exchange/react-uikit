import {
    colors,
    fontSizes,
    fontWeights,
    space,
    sizes,
    lineHeights,
    radii,
    transition,
} from './constants';

export const defaultTheme = {
    colors,
    fontSizes,
    fontWeights,
    lineHeights,
    space,
    sizes,
    radii,
    transition,
    borderWidths: [0, 1],
    breakpoints: ['576px', '768px', '992px', '1200px'],
    shadows: {
        default: '0 0 4px rgba(0, 0, 0, 0.125)',
    },
};
